<%# partial for displaying merchant order items %>

<% if items && items != [] %>
  <% @type = items[0].status %>
  <section class="<%= @type %>">
    <h2><%= @type.capitalize %> </h2>

    <table class="table <%= @type %>">
      <thead>
        <tr>
          <th>Order Number</th>
          <th>Product</th>
          <th>Date Ordered</th>
          <th>Status</th>
          <th>Quantity</th>
          <th>Subtotal</th>
          <th>Change Status</th>
        </tr>
      </thead>

      <tbody>
        <% items.each do |item| %>
        <tr>
          <td><%= link_to item.order_id, confirmation_order_path(item.order_id) %></td>
          <td><%= link_to item.product.name, product_path(item.product) %></td>
          <td><%= item.created_at.strftime('%b %-d, %Y') %></td>
          <td><%= item.status %></td>
          <td><%= item.quantity %></td>
          <td><%= number_to_currency(item.price) %></td>
          <td>
            <%= form_tag(change_status_path(item.id), method: :patch) do %>
              <%= select_tag :status, options_for_select(["pending", "paid", "complete", "canceled"], item.status) %>
              <%= submit_tag "Change Status" %>
            <% end %>
            <%# link_to "change status path", '#' %>
            <%# select_tag "status", options_from_collection_for_select(["pending", "paid", "complete", "canceled"], item.status) %>
            <%# link_to "Update Status", change_status_path %>
          </td>
        </tr>
        <% end %>
        <tr>
          <td><strong><%= message %></strong></td>
          <td><%= number_to_currency(@merchant.revenue(state)) %></td>
        </tr>
      </tbody>
    </table>
  </section>
<% end %>
